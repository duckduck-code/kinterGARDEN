VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# Optional — set to "true" only if you want the "sample data preview" login
# bypass available on a deployed build, not just local dev. Leave unset to
# keep it local-only (recommended). See README for details.
# VITE_ALLOW_PREVIEW=true
